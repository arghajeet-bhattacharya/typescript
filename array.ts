const carCompany: string[]= ['audi', 'bmw', 'ford'];

carCompany.push('tesla');

carCompany.map((element: string): string => {
    return element  
})

const car1 = carCompany[0];


const carModel: string[][] = [
    ['mustang'],
    ['model s'],
    ['a8']
]

const a2: (string | boolean)[] = ['a1', 'b2', false];


