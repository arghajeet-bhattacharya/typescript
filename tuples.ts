const myCar = {
    maker: 'tesla',
    colour: 'red',
    speed: 40,
    isWarrenty: true
}

const mybmwTuples: [string, string, number, boolean] = ['bmw', 'black', 50, true];

// type alias
type CarProperty = [string, string, number, boolean];

const myAudiTuples: CarProperty = ['audi', 'red', 70, false];

